import normalize from '../utils/normalize';

export const inputSizes = {
  width: normalize(370),
  height: 60,
};

export const buttonSizes = {
  width: normalize(300),
  height: 80,
};

export const squareLogoSizes = {
  width: normalize(391),
  height: normalize(160),
};
